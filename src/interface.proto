syntax = "proto3";

package ping;
option go_package = "https://github.com/frmo24042/P2P-DISYS;ping";


service Ping {
    rpc ping (Request) returns (Reply) {}
}

message Request {
    int32 id = 1;
    int32 lamport = 2;
}

message Reply {
    int32 amount = 1;
    int32 lamport = 2;
}